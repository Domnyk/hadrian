image: docker:latest

services:
   - docker:dind

stages:
  - build
  - test
# 
# variables:
#   POSTGRES_HOST: postgres
#   POSTGRES_USER: postgres
#   POSTGRES_PASSWORD: "postgres"
#
before_script:
  - docker login -u $CI_REGISTRY_USER -p $CI_JOB_TOKEN $CI_REGISTRY_IMAGE

Build container:
  variables:
    IMAGE_NAME: $CI_REGISTRY_IMAGE/$CI_COMMIT_REF_NAME:latest
  stage: build
  tags:
    - docker
    #  - npm install -g brunch
    #  - cd ./assets && npm install && cd ..
    #  - mix local.hex --force
    #  - mix local.rebar --force
    #  - mix deps.get --only $MIX_ENV
    #  - mix ecto.create
    #  - mix ecto.migrate
  script:
    - docker build -f Dockerfile.build -t $IMAGE_NAME .
    - docker push $IMAGE_NAME

Test:
  variables:
#     MIX_ENV: test
#     POSTGRES_DB: hadrian_prod
  stage: test
#   before_script:
#     - apk --update add postgresql-client
#     - mix local.hex --force
#     - mix local.rebar --force
#     - mix deps.get --only $MIX_ENV
#     - mix ecto.create
#     - mix ecto.migrate
  script:
    -  

